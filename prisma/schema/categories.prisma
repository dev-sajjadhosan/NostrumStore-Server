enum CategoryStatus {
  BAN
  UNBAN
}

model Categories {
  id          String         @id @default(uuid())
  name        String         @unique
  image       String
  description String?
  status      CategoryStatus @default(UNBAN)
  medicines   Medicines[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
